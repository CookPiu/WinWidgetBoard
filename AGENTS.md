# WinWidgetBoard 项目执行规则

本文件适用于仓库全部目录。项目目标是一个可靠、克制、可维护的 Windows 小工具，不是通用卡片平台或正式商用套件。

## 1. 当前产品边界

轻量核心版只主动维护七项能力：

1. `LauncherHost` 任务栏入口；
2. `WorkspacePanel` 面板；
3. 基础响应式布局；
4. 本地便签；
5. 天气及位置设置（两个可选数据源）；
6. 硬件监控（[ADR-0028](docs/adr/0028-system-monitor-scope-and-sensor-tiers.md)）；
7. Token 用量（[ADR-0030](docs/adr/0030-token-usage-card.md)）。

计时器、待办、剪贴板、日历、第三方插件、云同步、账号体系、遥测和内核级传感器驱动均为延期项。已有占位或基础合同可以保留，但不得继续扩展，除非用户明确重新批准范围。新增卡片品类需要 ADR 级再批准。

## 2. 开工前阅读

每个任务只需阅读：

1. `README.md`；
2. `docs/status/implementation-status.md`；
3. 与本次修改直接相关的一份需求或技术文档；
4. 与本次决策直接相关的 ADR。

不再要求每次通读全部 ADR、历史工作包或累计验收记录。Git 历史负责保存已经完成的实现过程。

## 3. 简化优先

- 优先复用现有类型、服务和协议，不为假想的第二种实现提前抽象。
- 一个小功能不应自动新增项目、进程、通用框架或永久后台服务。
- 不实现“以后可能用到”的插件、权限、同步、市场、遥测和企业管理能力。
- 修改集中式大文件时，不再继续塞入无关职责；优先提取现有逻辑，再增加行为。
- XAML code-behind 只保留控件事件、焦点和视图协调；业务状态放入 ViewModel 或服务。
- 不因追求形式完整而同时更新多份内容相同的文档。

## 4. 必须保留的安全边界

- 禁止向 `explorer.exe` 注入 DLL、Hook 或 XAML。
- 禁止依赖未公开的任务栏视觉树名称。
- `LauncherHost` 不加载 WinUI、SQLite、HTTP 或插件运行时。
- `WorkspacePanel` 不直接访问 SQLite；通过 `CoreBroker.Client` 使用版本化 IPC。
- IPC 输入必须验证版本、长度、枚举和值域。
- 用户凭据（当前只有天气 API Key）以 DPAPI 加密落库，只作为单次请求头发往白名单内的厂商域名，经 IPC 只写不读；不得进入 URL、日志、错误文本或命令行。
- 数据库迁移必须可重复，并在破坏性升级前具备备份恢复路径。
- 后台工作必须支持取消、超时和不可见降频。
- 基础功能不要求管理员权限。
- 默认无遥测、广告、新闻流、强制账号或永久剪贴板历史。

轻量化不等于放弃数据安全、进程隔离和用户隐私。

## 5. 风险分级验证

不再对每个小改动执行完整 Debug/Release、全项目构建、全部 UIA 和完整设备矩阵。

### L0：文档与无行为配置

- `git diff --check`；
- 文档链接或格式检查。

### L1：纯逻辑或局部服务

- 运行相关单元测试；
- 必要时构建一个受影响项目和一个配置。

### L2：UI、IPC、数据库或进程交互

- 相关单元/集成测试；
- 受影响可执行文件的 Release 构建或 smoke；
- 一条针对本次风险的真实桌面流程。

### L3：发布候选

仅在准备发布时执行：

- Debug/Release 全解决方案构建；
- 全量测试；
- 多显示器、DPI、高对比度、减少动态效果和键盘矩阵；
- 性能、长稳、恢复、安装和签名验证。

修复缺陷应有最小复现或回归证据。性能结论仍必须提供真实测量条件。

## 6. UI 变更规范

- `docs/02-ux-design-spec.md` 是当前 UI、视觉、密度、组件结构和动效的权威规范。
- 涉及可见 UI 的任务开工前必须阅读该规范，并使用 `docs/templates/ui-change-template.md` 作为检查表。
- UI 评审必须记录 `Before / After / Why`，并固定主题、DPI、窗口、语言、数据状态和基线提交。
- 优先复用 `WorkspaceVisualStyles.xaml` 中的 token 和 Style；不得在页面局部建立第二套颜色、间距、字号、圆角或阴影。
- 搜索、单组按钮和普通状态不得独占常驻整行；低频操作使用渐进披露，主要内容空间优先。
- 动效必须有反馈、状态或空间关系目的，可中断，并支持减少动态效果；高频点击、输入、拖动和缩放不得增加等待。
- 普通 UI 修复只把模板用于任务、PR 或实现过程检查，默认不提交填写后的副本。
- UI、布局和动效属于 L2；除相关测试与 Release 构建或 smoke 外，必须有一条针对风险的真实桌面流程和同条件截图。

## 7. 文档规则

- `README.md`：产品定位和入口。
- `docs/01-product-requirements.md`：当前轻量范围。
- `docs/status/implementation-status.md`：当前事实、债务和下一步，不累计逐包流水账。
- `docs/adr/`：只记录高成本、难回退的架构或产品决策。
- `CONTRIBUTING.md`：对外贡献流程、范围边界和构建陷阱；`SECURITY.md`：漏洞披露渠道与威胁模型。
- 普通功能和修复不再创建独立工作包文档；需求编号、提交和测试已足够追踪。
- 已完成历史保留在 Git 和 `docs/history/` 的简表中。

## 8. Git

- 默认分支为 `main`。
- 一次提交只解决一个清晰目的。
- 提交格式：`type(scope): summary`。
- 提交前运行与风险等级匹配的检查，不为满足形式重复运行无关矩阵。
- 不提交缓存、构建输出、数据库、日志、转储、密钥或签名证书。
- 仓库已公开且采用 MIT（[ADR-0039](docs/adr/0039-mit-license-and-public-repository.md)）：提交信息、issue、PR 和粘贴的日志截图中不得出现 API Key、session token、含用户名的本机绝对路径、便签内容或精确位置。
- 隔离验证输出使用系统临时目录或统一 `artifacts/validation`；验证结束后清理。

## 9. 完成定义

任务完成需要：

- 范围没有超出轻量核心版；
- 行为正确且有与风险匹配的验证；
- 必要的需求或状态文档已更新；
- 没有新增无用抽象和意外生成文件；
- 工作树中的用户既有修改得到保留。
