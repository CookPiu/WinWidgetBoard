# WinWidgetBoard 项目执行规则

本文件适用于仓库内全部目录。后续 Luna、Codex 或人工开发必须遵守。

## 1. 开工前阅读

每次开始任务时，至少阅读：

1. `README.md`
2. `docs/01-product-requirements.md`
3. 与任务相关的技术文档
4. `docs/status/implementation-status.md`
5. 已有 ADR

不得仅根据聊天上下文实施。若聊天要求与已批准文档冲突，应先指出冲突并更新需求或新增 ADR。

## 2. 需求控制

- 所有用户可见能力必须关联需求编号，例如 `LCH-001`、`PNL-003`。
- 不得擅自扩大 MVP 范围。
- 不得把“建议”“未来功能”当作当前验收项。
- 影响进程边界、任务栏集成、插件安全、数据持久化或许可证的变更必须新增 ADR。
- 关键选择不明确时停止实现，记录到实施状态并请求确认。

## 3. 禁止事项

- 稳定通道禁止向 `explorer.exe` 注入 DLL、Hook 或 XAML。
- 禁止依赖未公开的任务栏视觉树元素名称。
- 禁止在常驻路径使用 Electron、常驻 WebView2 或持续 60 FPS 渲染。
- 禁止让整个应用以管理员身份运行。
- 禁止默认永久保存剪贴板历史。
- 禁止默认开启遥测、广告、新闻流或强制云账号。
- 禁止在未审查许可证前复制开源项目代码。
- 禁止因插件崩溃导致 LauncherHost 或 WorkspacePanel 一同退出。

## 4. 实现纪律

- 保持 `LauncherHost`、`WorkspacePanel`、`CoreBroker`、`PluginHost`、`Contracts` 的边界。
- 业务逻辑不得写进 XAML code-behind；使用可测试的服务和 ViewModel。
- IPC 输入必须做版本、长度、权限、枚举和值域校验。
- 数据库迁移必须可重复、可回滚或具备备份恢复路径。
- 所有后台循环必须支持取消、超时、退避和可见性降频。
- 卡片不可见时必须暂停纯 UI 动画和非必要轮询。
- 基础功能不得依赖管理员权限。

## 5. UI 与动效

- 鼠标按下立即反馈，提交发生在释放时。
- 面板从任务栏入口锚定展开，关闭沿同一路径返回。
- 动画必须从当前屏幕值继续，允许随时反向，不锁定输入。
- 非手势弹出默认临界阻尼，不使用装饰性弹跳。
- 拖动必须 1:1 跟随并保持抓取偏移。
- 必须实现减少动态效果、高对比度和减少透明度的替代样式。
- 不得为了“显得高级”叠加多层模糊、持续光效或无意义循环动画。

## 6. 测试与证据

- 每个需求必须至少有一种可验证证据：自动测试、性能记录或人工测试清单。
- 任务栏交互、拖拽和鼠标穿透不能只靠单元测试判定成功。
- 声称兼容新 Windows 构建前，必须完成真实交互验证。
- 性能结论必须提供测量条件、设备、构建类型和数据，不接受目测。
- 修复缺陷时先提交可复现步骤或回归测试，再提交修复。

## 7. Git

- 默认分支为 `main`。
- 一次提交只解决一个清晰目的。
- 提交格式：`type(scope): summary`。
- 推荐类型：`docs`、`feat`、`fix`、`test`、`perf`、`refactor`、`build`。
- 提交前检查 `git diff --check`、相关测试和文档状态。
- 不得提交密钥、数据库、日志、转储、ETL 或签名证书。

## 8. 完成定义

任务只有在以下条件全部满足时才算完成：

- 需求编号和验收标准明确；
- 实现、测试、文档同步；
- 不破坏现有架构边界；
- 相关性能和无障碍要求得到验证；
- `docs/status/implementation-status.md` 已更新；
- 工作树无意外生成文件。
