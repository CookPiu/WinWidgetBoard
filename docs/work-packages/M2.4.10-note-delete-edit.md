# M2.4.10 NTE-001 多便签删除与安全编辑

状态：已完成代码、自动测试和当前桌面删除/安全切换回归
关联需求：NTE-001、NFR-REL-002、NFR-A11Y-002
关联 ADR：ADR-0010、ADR-0012、ADR-0013、ADR-0016

## 目标

在现有多便签列表中继续安全打开便签编辑，并增加带 revision 保护和二次确认的
删除能力。删除失败时不得丢失当前编辑内容或列表项。

## 实现范围

- 搜索结果携带 `UpdatedAtUtc`，删除请求使用该 revision；
- 当前便签增加删除入口，删除成功后清空编辑器并安全切换到剩余第一条便签；
- 列表项增加删除入口，列表项点击继续打开现有编辑器；
- 有未保存草稿、正在加载/保存或 CoreBroker 不可用时阻止删除；
- 增加中英文删除确认、状态文案和 UI Automation 名称；
- 不新增 IPC 方法、数据库表、版本历史、删除撤销或键盘移动功能。

## 自动验证

- `UT-NOTE-032 [NTE-001]`：当前便签删除携带最新 revision 并清空编辑器；
- `UT-NOTE-033 [NTE-001]`：未保存草稿时拒绝删除；
- `UT-NOTE-034 [NTE-001]`：删除失败保留当前便签和内容；
- `UT-NOTE-035 [NTE-001]`：列表删除成功移除结果并携带 revision；
- `UT-NOTE-036 [NTE-001]`：列表删除失败保留结果；
- Debug UnitTests：138/138；
- WorkspacePanel Debug x64 隔离输出构建：0 警告、0 错误；
- Windows PowerShell 5.1 与 PowerShell 7 脚本语法解析。

## 真实桌面验证

脚本使用独立验收实例和显式临时数据库，可与生产 CoreBroker/WorkspacePanel 并存：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File 'D:\Projects\WinWidgetBoard\scripts\Test-NoteDeleteInteraction.ps1'
```

预期输出：

```text
REAL-NOTE-DELETE-PASS current+list
```

2026-08-10 当前 Windows 11 桌面已产生上述通过证据。脚本创建两条便签，删除
当前和列表便签，并确认编辑器安全切换及删除结果；结束时只清理显式临时数据目录
和自己启动的进程。
