# 实施状态

最后更新：2026-09-02
当前策略：轻量核心版
当前代码基线：本文件所在提交

## 1. 当前结论

入口、面板、基础布局、便签、天气、硬件监控和 Token 用量已经形成可运行链路。新卡片仍需 ADR 级再批准（[ADR-0022](../adr/0022-lightweight-core-strategy.md)），平台能力不扩展——第三方插件经评估后仍不实现，理由见 [ADR-0030](../adr/0030-token-usage-card.md) 背景一节。当前优先修复核心工作台的可用性、视觉密度和维护债务。

WorkspacePanel 已完成“静谧画布”视觉收口：减少多层边框和外围留白，把标题、日期、搜索与全局操作合并为单行头部，并统一缩小字号、间距、控件和网格尺度。面板使用系统 Desktop Acrylic、强调色微光和圆角，卡片使用更实的系统语义表面与轻量阴影；便签默认只保留标题、正文、新建和更多操作，Markdown、复制、删除及恢复命令按状态渐进呈现。面板底板固定在最终位置，当前已实现卡片由自身 `RenderTransform` 驱动、朝任务栏胶囊方向沿独立曲线折页归位；翻转轴由胶囊中心指向卡片目标中心，铰点位于朝向胶囊的卡片边缘。合成层镜像（`RedirectVisual` / `CompositionVisualSurface`）已废弃——它们会把卡片子树的 XAML 命中几何整体下移一个网格行且不恢复。首次打开等待可见卡片完成实现，折页期间用 `BitmapCache` 避免逐帧重栅格化，反向保留当前速度，落地精确回到恒等值。动效帧源跟随当前 XAML 合成目标而非固定 16 ms 定时器，可自动适配窗口所在显示器的刷新率；卡片动效只改合成层属性，不改变布局、便签、天气或 IPC 数据合同。

## 2. 核心能力

| 能力 | 当前状态 | 仍需处理 |
| --- | --- | --- |
| 任务栏入口 | 已改为嵌入任务栏条带的自适应信息条；位置、内容、硬件胶囊开关与快捷键（四个预设加自定义录制）全部在面板设置的「任务栏入口」分类里改，入口监视 HKCU 键即时应用并把热键注册结果写回，右键菜单只剩「打开/关闭面板」与「退出」；天气模式显示图标 + 温度，并画与卡片同源的平面插画；单台参考机的入口到面板延迟与空闲占用已测量 | 发布前补完整显示矩阵和多设备性能 |
| 面板 | 已实现 WinUI 壳层、Desktop Acrylic、系统强调色层级、圆角阴影、锚定动效、关闭和模态保护；关闭改为隐藏并常驻进程，重开约 27～42 ms | 继续拆分集中式 code-behind，并在真实使用后校准细节 |
| 基础布局 | 已实现 2/4/6 列、拖动、持久化、恢复和撤销 | 只修缺陷，不扩展复杂编辑 |
| 便签 | 已实现 CRUD、搜索、Markdown、自动保存和安全删除；编辑区使用内容优先与渐进操作 | 只维护核心旅程 |
| 天气 | 已实现 Open-Meteo、订阅、Windows 自动位置、手动位置和重启恢复设置；首次前台授权后每次冷启动读取一次系统位置，失败保留手动位置；面板关闭时按小时后台刷新，任务栏入口可经 `weather.summary.get` 显示读数；显示单位可选公制/英制并随设置持久化，线上数值恒为公制、两端组装时换算 | 跨重启 payload 缓存延期 |
| 硬件监控 | 卡片与任务栏分段显示均已实现，显示项两处独立可配置并落盘；任务栏分段列宽按内容测量、加余量后按 12 DIP 量化并以 72 DIP 为下限——同字符数的数字抖动不动格，真正变宽的读数（如 `4.48 GHz`）立即重排而不再省略号截断；公开 API 层覆盖 9 项读数（含 CPU 频率），采样按需进行，任务栏硬件胶囊启用时每秒采样并刷新一次；温度与风扇由监控程序的共享内存补齐（HWiNFO 三项、Core Temp 仅 CPU 温度，同项 HWiNFO 优先），无覆盖该项的源时卡片与入口都不显示该行、来源要求写在硬件监控设置页，判断逐项进行（[ADR-0033](../adr/0033-hwinfo-shared-memory-sensor-source.md)、[ADR-0034](../adr/0034-core-temp-as-a-second-sensor-source.md)） | 本产品仍不分发任何内核驱动（[ADR-0029](../adr/0029-drop-the-bundled-sensor-driver.md)、[ADR-0031](../adr/0031-cpu-clock-from-per-core-counters.md)）；真实 HWiNFO 块的解析尚未在真机上验证 |
| Token 用量 | 卡片已实现，读 Claude 与 Codex 的本机会话记录：两者格式与口径不同，Claude 按 `(requestId, messageId)` 去重、Codex 取会话累计量的相邻差分；分总览页与厂商页，设置页可启用/禁用厂商；以当日花费为主视觉，背后铺当日累计花费曲线（0 点到现在铺满卡宽，指针悬停显示该小时的累计金额与本时计费，[ADR-0036](../adr/0036-token-usage-spend-curve-card.md)）；两行高的卡片另有总 token / 响应数两块 KPI 与厂商或模型拆分计量条；下列今日计费/输出/缓存读取/缓存命中率，各 token 类型带自己那份金额，金额按公开 API 价折算并标 `≈`，无价模型不计入并以 `+` 标为下限、全部无价时在花费位置说明；价表每日从 LiteLLM 开源价表条件拉取（用户可关，[ADR-0035](../adr/0035-daily-token-pricing-sync.md)），内置表带核验日期作兜底；内容按卡片格数披露（S 一项读数，M/W 仅花费与页签，L 起全部）；卡片不可见时完全不读盘 | 不展示订阅配额（本地记录无法判断自身是否仍然成立，见 [ADR-0030](../adr/0030-token-usage-card.md) §2.2）；无请求级延迟；新增厂商的门禁见同一 ADR |

## 3. 已有基础设施

- 当前用户 Named Pipe、握手、心跳、重连和版本化 Envelope；
- CoreBroker SQLite migration、备份边界和 revision 保护；
- 卡片不可变快照、连接级订阅、有界背压和 UI dispatcher；
- 集中 Provider 调度、超时、退避、可见性暂停和 fatal fault 边界；
- 验收进程身份与临时数据库隔离；
- 双语资源和基础无障碍锚点。

这些能力进入维护状态，不再继续平台化。

## 4. 最近完成证据

当前提交的完成证据：

- **Token 用量卡片改版：累计花费曲线 + 双 KPI + 拆分计量条（2026-09-09，[ADR-0036](../adr/0036-token-usage-spend-curve-card.md)）**。
  Before：24 小时滚动趋势条与「今日花费」口径不一，最右一格永远是当前小时；五条读数行权重相同，
  总 token 与平均每次计费要读者自己算；两行卡片读数行之下空一截。After：曲线为 0 点到现在的累计花费，
  铺满卡宽、右缘恒为「现在」，按单调三次样条绘制（`TokenUsageSpendCurveShape`，`UT-TOKUSE-127`～`130` 钉住不回落、
  不过冲、所绘与所读同一曲线）；指针悬停时 1 DIP 竖线 + 圆点沿曲线连续跟随，`HH:00 ≈$累计 · 本时 计费` 标签写指针所在
  小时的真实读数，靠右缘翻转，只动 `Translation` 无动画，编辑模式下不接收指针；L 起显示总 token（注缓存读取）与响应数（注平均每次计费）两块，
  响应数不再作为读数行；两个最大切片分列一条 3 DIP 计量条两端并各带金额；载荷新增 `spendCurve`/`totalTokensText`/
  `averageBilledPerRequestText`、切片 `costText`，删除 `trend`；聚合改按本地小时分桶并逐小时计价。
  证据：Release UnitTests 586/586（新增 `UT-TOKUSE-120`～`130`，删除 `043/044`，`022/023/029/031/064/065/090`～`096`
  改为按曲线与 KPI 断言）；`WorkspacePanel --smoke-test` 与 `CoreBroker --pipe-handshake-smoke-test` 退出码均为 0；
  已按 Release x64 重新安装。首次安装后面板打不开：资源键 `TokenUsageSpendCurve.Now/.HourTokens` 与曲线控件的
  `x:Uid="TokenUsageSpendCurve"` 同前缀，被资源加载器当作要设到控件上的属性，面板一开即以 `0xc000027b` 崩溃
  （编译与 `--smoke-test` 都不报，只有 `%LOCALAPPDATA%\CrashDumps` 里的转储字符串说明原因，规则已记入 `CLAUDE.md`）；
  改为 `TokenUsageCurve.*` 后仍崩：第二个原因是 `LayoutCycleException`——曲线 `Path` 直接挂在模板 Grid 下，
  每次 `SizeChanged` 重建几何，描边把期望尺寸撑大半像素，布局永不收敛；转储里没有这段文字，临时的
  `Application.UnhandledException` 钩子第一次运行就报出来了。现改为所有图形放进 `Canvas`（期望尺寸不随子元素变）。
  复现与验证走隔离栈：停掉已安装实例后用 `Start-TestBroker`/`Start-TestPanel`（临时数据目录、新实例 id）启动，
  Broker 照常扫描本机会话记录，30 s 后面板存活、UIA 读到 `TokenUsageCostText = ≈$51.80`、`TokenUsageTotalTokensText = 39.4M`，
  应用日志无崩溃；两次排查经过与规则已记入 `CLAUDE.md`。**未复验**：目视核对（浅色/深色下曲线可见、指针跨右缘时标签翻转、
  编辑模式下按住大字可拖动）仍未做——本机另一全屏程序挡住桌面截图；下次打开面板时按
  [ADR-0036](../adr/0036-token-usage-spend-curve-card.md) 门禁三项逐一核对。
- **便签卡片交互与布局整改（2026-09-02，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文，隔离栈脚本复跑）**。
  Before：撤销/重做按钮的 `x:Load` 绑在含 `!_isSaving` 的 `CanUndo` 上，每次 500 ms 自动保存都卸载再重建；
  每次按键入一条撤销快照，20 步只够四个词，且 Ctrl+Z 走文本框自己的栈、回来时被记成新编辑并清掉 redo；
  列表按钮只会打开不会关，空查询下展开的切换器没有出口；预览切换、搜索层、预览/编辑按钮互换都从
  `RootGrid` 按名取第一个命中，第二张便签卡永远停在编辑态；输入完半秒内点新建/打开/删除得到「请先保存当前
  草稿」；标题/正文/搜索词无 `MaxLength`，超限后 Broker 拒绝、重试永远失败；首次加载失败后编辑器仍可输入，
  空修订的草稿走 `Create` 撞主键；修订冲突只有一个必然失败的「重试」；`MarkdownPreviewBlocks` 每键全文重排
  五个正则、不看预览是否可见；预览独占一行时星号行空着仍占高度，预览被顶到卡底上方留白；正文 `MinHeight=120`
  在一格卡片上嵌套两根滚动条；「已保存到本地」永不回落成常驻行；单行卡片上 180 高的列表把编辑器推出视口。
  After：撤销不等保存、一步 = 两次保存之间同一字段的一串编辑（`UT-NOTE-045/047`）；Ctrl+Z/Y/Shift+Z 经
  `PreviewKeyDown` 交给编辑器历史，光标落在改动区域末尾（`NoteCaretPlacement`，`UT-NOTE-050`）；列表按钮开合、
  搜索框 Esc 关闭、删除后切换器保持原开合状态；便签处理程序一律从触发元素上溯到 repeater 元素解析所在卡片，
  预览状态同步到每张便签副本并在新卡片 `ElementPrepared` 时对齐；新建/打开/删除先 `SaveNowAsync` 冲刷草稿
  （`UT-NOTE-043/044/052`），切换途中到达的编辑不再记到被离开的便签上（`UT-NOTE-053`）；`MaxLength` 按契约封顶
  并由 `UT-NOTE-049` 锁定；加载失败与修订冲突给「重新加载」、冲突不再提供重试（`UT-NOTE-048`）；预览块按正文引用
  缓存、仅预览可见时发布；编辑器与预览共用星号行；正文去掉最小高度；Saved 3 s 后回落 Ready（`UT-NOTE-046`）；
  单行卡片浏览时独占卡片、列表上限 64（`UT-NOTE-051`）；撤销/重做/预览切换/重试成功不再在面板状态行复述；
  列表按钮补上 ToolTip，孤儿键 `ListNotesButtonToolTip` 与五条复述用状态串删除，两份 resw 各 386 条。
  证据：Release UnitTests 577/577（新增 `UT-NOTE-043`～`053`，`UT-NOTE-020/022` 改为按保存分步）；
  `WorkspacePanel --smoke-test` 退出码 0；四个真实桌面便签脚本全部通过（见下方桌面流程条目）。复跑中抓到并修掉
  两处：`ItemsRepeater` 对 `x:Bind` 模板不设 `DataContext`，枚举便签卡改走 `TryGetElement`/`GetElementIndex`；
  `TextBox.TextChanged` 在赋值后的下一拍才触发，清空搜索框时的抑制旗标对它无效，改为「空框且查询已空即无事可做」，
  否则搜索后重开列表会被空查询作废。**未复验**：两张便签卡同时在板上的真实桌面流程（脚本用默认布局，只有一张）、
  一格（`M`/`W`）卡片上切换器独占的实拍；
- **Token 定价每日同步（2026-09-02，[ADR-0035](../adr/0035-daily-token-pricing-sync.md)）**。Before：用户当天把
  Claude Code 切到新发布的 `claude-fable-5-1`，价表没有这个 id，精确匹配拒绝猜测，当日全部用量无价、
  `TodayCostUsd` 为 0，卡片把「今日花费」整块隐藏且不说明——ADR-0030 后果里写的「价表静默过期」原样兑现。
  After：Broker 内一条后台循环每日从 LiteLLM 开源价表（GitHub raw，MIT）条件 GET 一次（`If-None-Match`，
  失败 1 小时重试），`Utf8JsonReader` 流式过滤到 `claude-*`/`gpt-*` 一方条目，同步价优先、内置表兜底
  （`TokenUsageRateBook`），落盘一行过滤后的价格 + ETag + 时间（迁移 9）；设置页新增「每日同步公开定价」
  开关（默认开，`tokenusage.settings.syncPricing`）与价表更新时间；全部无价时卡片在花费位置说明「N 个模型
  无价格」而不是空白；内置表补入 `claude-fable-5-1`/`claude-mythos-5-1`（缓存读取 $0.25/M，按官方定价页核对）。
  证据：Release UnitTests 566/566（新增 `UT-TOKUSE-110`～`116`：一方 id 过滤、缺失缓存字段按厂商倍率补、
  同步价优先、节奏、落盘往返、内置表、一次尝试的三种结果）；安装后生产库 `user_version = 9`，`token_usage_pricing` 一行
  15,290 字节、带 ETag、`fetched_at_utc = 2026-09-02T01:47:34Z`（安装后数秒内完成首次拉取）；实机截图：
  卡片重新显示 `≈$29.39`，缓存读取 30.0M ≈ $7.51 与 Fable 5.1 的 $0.25/M 一致，设置页显示
  「价表更新于 2026/9/2 9:47（来源：LiteLLM 开源价表）」。开关旁另有「立即同步」按钮
  （`tokenusage.pricing.sync`，异步、不持路由锁、不受开关约束）：实机点击后状态行报「价表已是最新，没有变化」，
  库里 `fetched_at_utc` 由 01:47 前进到 01:58 而 ETag 不变——这同时验证了 304 路径；重装重启时因缓存未满
  24 小时**没有**重复下载，节奏按预期。**未验证**：关闭开关后不再自发访问端点，只由代码路径覆盖，未抓包；
- **面板质量整改一轮（2026-09-02，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，每批安装后
  经 `PrintWindow` 实机截图与 UIA 复核）**。Before：头部状态行永久残留上一条消息（「设置已保存。」几小时后
  仍在）且编辑提示被 220 DIP 截断成「…删除...」；便签卡底部常驻 10 DIP 提示；硬件卡「CPU 温度 需 HWiNFO/Core Temp」
  把安装指引摆在读数位；七根满宽进度条把卡面读成表格线；天气「晴」字形是一颗圆点、逐小时六颗同样的圆点读作项目符号；
  页脚「更新于 15:30 Weather data by Open-Meteo.com」中英混排；L 卡今日区间因快照路径没发布标签可见性而只剩两个裸数；
  Token 卡空闲小时画成一排短横、输出行多一条含义不同的 meter；添加卡片对话框是系统默认白框、确认按钮用图标字体样式渲染文字；
  硬件监控设置每行两枚箭头、两份八行列表把保存行推到两屏外；默认板上躺着三张「暂不可用」占位卡；入口胶囊
  `CPU 4.48 GHz` 被固定 72 DIP 槽位截成 `CPU 4.48...`，两行都标 `CPU`；入口右键菜单是一棵设置树、快捷键只有预设。
  After：状态行 8 s 后合成器淡出并清空文本（行高保留，卡片不位移）；便签 Ready 态无文案；无传感器源的行整条不显示、
  要求写在设置页；仪表条改为极淡轨道的 3 DIP 样式；「晴」为光环加日盘；署名本地化；今日区间在所有尺寸都是两值加斜线；
  空闲小时不画、只有命中率保留 meter；两个对话框共用面板材质与 16 DIP 圆角、关闭按钮不再整宽；指标列表拖拽重排并
  默认收起；占位卡退出面板与选择器，选择器改列全部卡片、同类型可重复添加（`demo.weather#2`，快照按基础身份扇出，
  Broker 零改动）；入口列宽按内容量化测量、频率行去掉冗余 `CPU` 标签；入口全部设置迁入面板「任务栏入口」分类，
  含自定义热键录制与占用回写，入口监视 HKCU 键即时应用。证据：Release UnitTests 559/559；LauncherHost
  `--smoke-test` / `--entry-visual-smoke-test` 退出码 0（smoke 改为断言「同字符数抖动不动格、更宽读数立即扩列」）；
  实机：重复天气卡添加后即拿到与主卡一致的实时数据、撤销后恢复；注册表写 `Content=0/1` 入口 2 s 内在
  `848↔920` 宽之间重排；写入自定义 `Ctrl+Shift+P` 后该组合打开面板而 `Ctrl+Alt+B` 不再响应，恢复预设后反之，
  `HotkeyActive` 全程为 1。**未验证**：指标列表拖拽重排只走过单元测试（`ListView.CanReorderItems` 对
  `ObservableCollection` 的内建行为），未做真实鼠标拖拽；自定义热键被其他程序占用时的「被占用」提示只由
  单元测试覆盖写回读取，未在真机制造冲突；
- **温度源改为多来源，新增 Core Temp（2026-09-01）**。Before：唯一的温度源是 HWiNFO，
  而它免费版的共享内存**到 12 小时自动关闭且要手动重新勾选**——自己用是小麻烦，作为分发出去的
  产品能力则是硬伤，不能让一项读数依赖用户每天去另一个程序里重新打勾。After：来源变成一组，
  Core Temp 补上 CPU 温度（免费、无时限、结构有官方 SDK 文档），同一项上 HWiNFO 优先——
  它的每条读数自带标签与所属传感器名，能说清选中的是哪个传感器，Core Temp 只给一组按核编号的浮点数。
  Why：**「有没有源」因此必须逐项判断**，不能再是一个全局布尔——只跑 Core Temp 的机器 CPU 温度有源、
  GPU 温度与风扇没有，把它算成「源在运行」会让后两项显示「此电脑无法读取」，等于告诉用户这台机器
  读不到，而其实换个源就能读。三处实现要点：CPU 温度取**最热的那颗核**而非平均（混合架构上八颗能效核
  闲着、一颗性能核满载时，平均值不描述芯片上的任何一点）；块里的**华氏度**与**距 TjMax 余量**两个标志位
  必须按固定顺序处理（先在同一单位内还原余量、再换算单位），读错的后果不是报错而是一个看起来合理的
  假数字——忽略前者把 60 °C 显示成 140，忽略后者得到一台越忙越凉的电脑；Core Temp 的块**没有时间戳**，
  共享段又因为我们持着句柄不随它退出而消失，因此改为每 30 秒丢句柄重开，它退出后下一次打开自然失败。
  命名空间规则同时收紧成一句话：**只允许全局优先、本地兜底**——HWiNFO 发布在全局所以只认全局，
  Core Temp 不带前缀发布所以两个都试且先试全局；反向回落是给同会话任意进程一个喂假读数的入口。
  证据：Release UnitTests 552/552（新增 `UT-SYSMON-080`～`086`，跑在测试自己写出的合成块上：
  取最热核、余量还原、华氏换算、两个标志同时生效的顺序、坏计数、缺席、逐项源判断）；
  CoreBroker 与 WorkspacePanel Release x64 构建 0 警告；实机 `CoreBroker --sysmon-smoke-test` 退出码 0，
  `sensor sources: cpuTemperature=absent gpuTemperature=absent fan=absent`；
  安装后生产路径实机播报 `CPU 温度 需 HWiNFO/Core Temp`。
  **未验证**：参考机既没装 HWiNFO 也没装 Core Temp，两个真实块的解析都只跑过合成块；
  另外四条免驱动路径（ACPI 热区、联想厂商 WMI、联想风扇 WMI、Afterburner 共享内存）已逐条实测为否，
  见下文表格——**要在零依赖前提下读到 CPU 温度只剩自签内核驱动一条路，该路已定为不走**（见下文）；

- **温度与风扇改由 HWiNFO 共享内存读取（2026-09-01）**。Before：这两项在任何机器上恒为
  `unavailable`，文案「此电脑无法读取」——Windows 没有对应的用户态 API，而本产品不分发内核驱动
  （[ADR-0029](../adr/0029-drop-the-bundled-sensor-driver.md)）。After：CoreBroker 只读打开
  `Global\HWiNFO_SENS_SM2`，按 SM2 布局取 CPU 温度、GPU 温度与风扇转速；HWiNFO 没在运行时新状态
  `needs-sensor-source` 让卡片说「需运行 HWiNFO」，而「此电脑无法读取」留给源在运行却确实没有这项读数的机器。
  Why：ADR-0029 当初连共享内存一起否掉，理由是「不能要求用户常驻另一个监控软件」；用户现在要求用 HWiNFO，
  这个前提就不成立了——常驻与否是用户自己的选择，本产品只需在他已经运行时读到、没运行时说清楚
  （[ADR-0033](../adr/0033-hwinfo-shared-memory-sensor-source.md)，不分发驱动这条不变）。
  该块是另一个进程的内存，因此按不可信输入处理：只认全局命名空间（本地命名空间任何进程都能创建同名段）、
  签名与版本与两段的偏移／元素大小／元素个数逐项按映射长度校验、不按块中的数字分配缓冲；
  选取用固定优先级表而不是出现顺序（一台机器同时发布好几个「像是 CPU 温度」的读数，按顺序取会在每台机器上
  选中不同的东西），`Distance to TjMAX` 这类以摄氏度为单位的余量读数一并排除；HWiNFO 崩溃后共享段仍在
  （句柄在我们手上），因此以它自己的 poll 时钟判活，60 秒不前进即断开重连。
  证据：Release UnitTests 545/545（新增 `UT-SYSMON-070`～`079`，其中解析、优先级、余量排除、空风扇位、
  坏头部拒绝、缺席、假死七项跑在测试自己写出的合成块上）；CoreBroker 与 WorkspacePanel Release x64 构建 0 警告；
  实机 `CoreBroker --sysmon-smoke-test` 退出码 0，`sensor source: absent`，
  温度/风扇三项报 `needs-sensor-source` 而其余九项照常 `ready`；生产路径实机播报 `CPU 温度 需运行 HWiNFO`。
  **未验证**：参考机没有安装 HWiNFO，真实块的解析、选取策略在真实标签上的表现、以及有读数时的显示，
  全部只跑过合成块。装上 HWiNFO（并勾选 Shared Memory Support）之前不要当作已验证；
  另注：HWiNFO 免费版的共享内存有 12 小时限制，到期自动关闭需手动重开——这条正是随后引入
  Core Temp 的原因（[ADR-0034](../adr/0034-core-temp-as-a-second-sensor-source.md)）；

- **编辑模式下关掉面板即放弃改动（2026-08-31，安装后实机验证）**。Before：面板带着编辑模式一起隐藏，下一次打开仍在编辑模式里，且带着上次没提交完的排布。After：隐藏时若仍在编辑模式且没有保存在途，就撤销回进入编辑模式前的布局、退出编辑模式，并在状态行说明。实机：`L` 尺寸进入编辑模式（边框高 668）→ 拖小天气卡至 `M`（332，未提交）→ 关掉面板 → 重新打开，`WeatherCardResizeFrame` 不存在（已退出编辑模式），状态行为「布局编辑已取消，已恢复进入编辑模式前的布局。」。**注意**：这条规则对所有隐藏路径一视同仁，焦点移开导致的隐藏同样会放弃未提交的改动；

- **天气抓取一直在失败（本轮定位并修复）**。`BuildRequestUri` 用 `Uri.EscapeDataString` 转义了 `current` / `hourly` / `daily` 三个变量列表，逗号变成 `%2C`；Open-Meteo 会把整串当成一个不认识的变量名，**只回时间轴、一个气象变量都不返回**，`ParseResponse` 随即抛出，每次刷新都是 `weather.invalid-response`，卡片只能靠上一次成功的载荷活着——表现就是预报整段消失、偶尔弹出「卡片无法刷新」。三个常量都是本文件里的字面量、只含 `[a-z0-9_,]`，本来就无需转义（位置与单位仍照常转义）。定位方式：把一份**真实**的 Open-Meteo 响应喂给真实 provider——修复前 `kind=Failed error=weather.invalid-response`，修复后 `kind=Success`、`hourly=12`、`daily=3`、今日最高在。已在 `UT-WEA-001` 里钉住查询串形状并断言其中不得出现 `%2C`。**未复验**：实机卡片在修复后仍未显示预报，怀疑面板仍持有修复前的快照，但我没能在使用者的桌面上确认；

- **天气与 Token 用量的尺寸阶梯重算（2026-08-31）**。天气：今日最高/最低那一行是本会话新加的，加的时候没有回头核高度预算——一行卡片约 98 DIP，气温 40 + 天气 20 + 区间 20 已花 80，次要读数还要 34，于是 `M` 上的次要读数被卡片自身裁剪切掉；现在次要读数改为需要两行网格（`L`/`XL`），`S` 因为只有约 170 DIP 宽而把区间画成「两值 + 斜线」、两格宽起带回名称。Token 用量：**今日花费改为所有尺寸都显示**，读数改为在花费与页签之后拿剩下的高度——原来是先摆两条读数、只有两行卡片才显示花费，等于把这张卡存在的理由排在明细之后。证据：Release UnitTests 535/535；`WorkspacePanel --smoke-test` 退出码 0；实机逐元素比对矩形，最小尺寸下地点/气温/天气/区间全部在卡内、次要读数与预报正确缺席、无一处越界；

- **卡片尺寸适配补齐 + 编辑模式让位（2026-08-31，同一参考环境，安装后实机验证）**。Before：只有天气与 Token 用量写明了尺寸阶梯；硬件监控把用户配置的全部读数一股脑铺开，一行高的卡片上后几条直接画进裁剪线；便签的四枚工具按钮在一格卡片上把便签本身挤没；编辑模式下卡片自己的设置齿轮与移除角标抢同一个右上角。After：硬件监控按高度花预算——一行约 98 DIP、两行约 266，一条读数约 29 DIP、带第二行明细再加 18、仪表条不额外计费，花完即停且不跳过后面矮的（列表顺序是用户排的优先级）；便签工具行在 `S` 上不出现；编辑模式下卡片的设置齿轮与便签工具全部隐藏。Why：卡片装不下的内容是被裁掉而不是滚动，所以溢出的不是「挤」而是连同下面的一切一起消失；按条数写死两头都会错——定死三条会把第三条画进裁剪线，把仪表条单独计费又会扣掉两行卡片本来放得下的一条，成本因此逐行按内容算。证据：Release UnitTests 534/534（新增 `UT-CARD-080`～`084`）；`WorkspacePanel --smoke-test` 退出码 0；实机确认编辑模式下硬件齿轮、Token 用量齿轮、便签弹出按钮三者同时消失、退出编辑后同时回来，硬件卡缩到一行后只显示放得下的读数且无一条被半截裁切。**未复验**：行成本按实拍重算（29 + 18）之后，两行卡片上第八条读数（磁盘占用）是否回归——重算前的实拍是 7/8，算术上新的预算容得下 8 条，但改完没有再实拍一次；

- **卡片缩放改为拖边框（2026-08-31，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，安装后实机手势验证）**。Before：进入编辑模式后每张卡片底部长出一条工具条（拖动柄 + 缩小 + 放大 + 移除），把「能做什么」写成文字，并且占掉卡片一行来放。After：卡片罩一层缩放边框——一圈细边加**右下角一枚手柄**（同时改两个轴：网格只有三档列宽两档行高，几乎每次改尺寸都是两轴同动，分开的边手柄最多只能走到一半），右上角一枚移除角标；八个卡片模板的工具条全部移除，`EditableCardSizes` 与两个步进按钮的点击处理随之删除。Why：**只有东南三个方向**，因为落位由确定性首次适配决定，从左或上缩放等于要求打包器尊重一个它随时会重排掉的原点；**吸附到五个尺寸**而不是自由像素，因为持久化的是 `sizeId`（ADR 0003 / 0014），指针挑列数与行数、落点吸到存在的那一档，平局吸窄的一档（拖过头是随后要纠正的动作）；**拖动只预览、松手才提交**，逐档提交会把一次手势压成三条撤销记录；没有 1 列 2 行这个尺寸，所以 `S` 拖高变 `L`。边框中间不接收指针输入，因此按住卡面仍是移动、卡内交互控件仍按原样隔离。五处首轮实机暴露的缺陷已修：**手柄辨识度过低**——根因不是尺寸而是位置，圆心压在卡片角上会被卡片自身的内边距裁剪裁掉四分之三，屏幕上只剩一个楔形；现在圆环放大到 20 DIP（3 DIP 底色环）并整个移进卡片内侧，命中区 36 DIP、吸附带 32 DIP 同步跟上；**观感单薄**——边框的抓点由扁平方块改为带底色环的圆点，移除角标由「每张卡片一个红叉」改为中性描边圆钮（红色留给指针真的落上去的那一刻），橡皮筋由三条被拉伸的图元改为真实的圆角描边矩形（放在 `Canvas` 里，改尺寸不牵动卡片网格）；**只有吸附、没有连续指引**——两个吸附点之间指针要走半张卡片而屏幕上什么都不动，现另加一条跟着指针连续走的橡皮筋（区域填充加正在拖的那条边的 3 DIP 实心条，全部按变换缩放，不走布局），与底下重排的板子构成「我拖到哪」「松手得到什么」两条信号；**拖动过程没有预览**——`ApplyDropPreview` 会把被拖卡片钉在已提交位置（移动手势必须如此，卡片由变换带着走），缩放沿用它等于整个手势没有反馈，现另起 `ApplyResizePreview`，全部卡片都按投影落位；**指针没有反馈**——三枚手柄各自把光标换成 ↔ / ↕ / ⤡，离开换回箭头，`ProtectedCursor` 在 UIElement 上是 protected，因此边框改为真实控件类型 `CardResizeFrame`。证据：Release UnitTests 529/529（新增 `UT-GRID-060`～`066` 与 `UT-GRID-067` 覆盖吸附、平局、无效组合与轴隔离；`UT-GRID-039`/`047`、`UT-CARD-013`、`UT-UI-003` 四条结构契约同步改写到新标记）；`WorkspacePanel --smoke-test` 退出码 0；安装后实机以合成指针拖天气卡片下边手柄：卡片由 `L` 缩到 `M`（边框高 684 → 348 物理像素）、状态行报「卡片尺寸已更新」；**按住不放时卡片已经变成新尺寸**（边框高在按键未松开时即为 348）；两个吸附点之间的定格截图显示橡皮筋底边正落在指针位置（距卡片下沿 120 物理像素，既不是 684 也不是 348）；一枚角手柄两轴同动经实机确认——向外拖 `L`→`XL`（701→1418 宽、行数已在上限故高不变），向内拖 `XL`→`M`（宽高同时缩小），而原来两枚边手柄的位置现在都回落为普通卡面（光标为箭头），再拖回 `L` 后逐小时与三日预报重新出现；光标以 `GetCursorInfo` 逐点比对：卡面为箭头、下边为 `IDC_SIZENS`、右边为 `IDC_SIZEWE`、右下角为 `IDC_SIZENWSE`；移除角标点击真实删除卡片、撤销真实恢复。真实桌面回归由**使用者在自己的交互式终端**跑通（2026-08-31）：`Test-CardDragInteraction.ps1 -ResizeDragCombination` 与 `.\Test-CardDragInteraction.ps1 -WithBroker` 两种模式均通过（两个开关互斥，`-ResizeDragCombination` 走不带 Broker 的确定性默认布局，脚本会当场拒绝合并）。改指 `*CardResizeFrame` 的三个脚本全部通过：`Test-CardDragInteraction.ps1`（两种模式）、`Test-AddCardInteraction.ps1`、`Test-CardLayoutHistoryInteraction.ps1`——即工具条移除后，卡片的拖动、添加/移除与布局撤销重做三条真实桌面路径都改由缩放边框驱动并验证过；

- **设置浮层切分类不再跳尺寸（2026-08-31，同一参考环境，UIA 逐分类量测）**。Before：滚动主体写的是 `MaxHeight`，于是每个分类按自己的内容把整张浮层撑成不同高度；从硬件监控切到只有一个开关的常规，浮层塌掉大半个高度，指针正瞄着的分类栏跟着从指针下面挪走，切换因此读作「顿一下」而不是「换一页」。After：主体写死 `Height="400"`，提交区所在行即使该分类没有提交区也保留 32 DIP；切换改为「走的一帧内消失、来的淡上来」，复用面板里所有渐进披露表面共用的 `SurfaceMotionCoordinator`（仅合成器属性、可打断、reduced motion 下扁平化），首次进入不放动画。不做交叉淡出：两段密排文字共用同一个单元格，互相透着彼此读成渲染故障。证据：安装后实机用 UIA 逐个选中四个分类，浮层关闭按钮的屏幕坐标恒为 `top=1521 height=64`（改动前会随分类移动）；Release UnitTests 521/521；`WorkspacePanel --smoke-test` 退出码 0。**未测**：淡入过程本身的逐帧证据——55 ms 的定格采样受截图链路自身开销影响，落在转场结束之后，没能抓到中间帧；

- **全局快捷键与开机自启开关（2026-08-31，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，安装后实机验证）**。Before：打开面板**只能用鼠标**——入口是 `WS_EX_NOACTIVATE` 覆盖窗口，永远不接收焦点，没有任何键盘路径；开机自启只由 `Install-WinWidgetBoard.ps1` 的启动文件夹快捷方式决定，应用内改不了，从发布包安装的人无从设置。After：入口注册全局快捷键（默认 `Ctrl + Alt + B`，按一次开、再按一次关，与点击胶囊同一动作），右键菜单新增「快捷键」子菜单，四个预设可切换或关闭；设置浮层新增**常规**分类，头部设置按钮改为落在这里，内含「开机自启」开关。Why：快捷键存在 HKCU 的 `LauncherPreferences` 而不是数据库——入口要先能打开面板，才谈得上在面板里配置怎么打开它，这也是既有入口设置一贯的存放位置，不新增 IPC 域；开机自启的唯一事实来源是系统自己（启动文件夹里有没有那个快捷方式），打开即复制安装器写好的开始菜单快捷方式，不另存开关位，因此用户在资源管理器里删掉它就等于关掉了设置。常规分类没有保存按钮：它请求系统做一件事，做成了或没做成，事后再「保存」无从谈起。**预设经实测换过一轮**：参考机上 `Ctrl + Alt + Space` 被微软输入法的半角切换占用、`Ctrl + Alt + W` 被另一程序占用，两者作为预设时都注册成了「什么也没发生」，现全部改为空闲的字母组合（标点键随布局移动，菜单会印出打不出的组合）。注册失败不静默降级，菜单标题写成「快捷键（被其他程序占用）」。证据：Release UnitTests 521/521（新增 `UT-GEN-001`～`006`）；LauncherHost `--smoke-test` / `--geometry-smoke-test` / `--entry-visual-smoke-test` 与 WorkspacePanel `--smoke-test` 退出码均为 0；安装后实机以 `SendInput` 按下 `Ctrl + Alt + B`：面板进程由无到有、再按隐藏且进程仍常驻、三按重新显示；设置浮层经 UIA 读到 `GeneralAutostartToggle` 为 `On`（与安装器刚写下的启动项一致），截图复核四分类栏与常规页排版无裁切。**未测**：注销后重新登录的实际自启（改动只是增删启动项快捷方式本身），以及英文 UI 下的实拍；

- **天气卡片插画重绘 + 今日最高/最低（2026-08-31，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，安装后实机截图复核）**。Before：插画的每个形状各自半透明，重叠处因此比两者都亮——太阳整个透过云显出来，两朵云之间有一道缝，条件字形（一摞圆）的每两圆相交都比云其余部分深一号；晴间多云的太阳还落在四列卡片的预报数字上，是一枚横穿文字的硬边圆盘；卡片只说「现在 28.4 °C」，不说今天到几度。After：每张插画是「天空一层 + 一个图形组」，组承担唯一的不透明度（深色 `0.18` / 浅色 `0.16`），组内形状不透明，因此云挡住太阳的部分是遮挡；八个条件字形各自改为**一条几何路径**（`F1` 非零填充、子路径同向），18 DIP 上是平的；图形整体收进右下角随圆角溢出；气温与天气描述之下新增「最高 X 最低 Y」，全尺寸显示。Why：观感问题的根因是 alpha 叠加而不是形状本身——同一处画两次半透明必然亮一档，改成组不透明度后重叠变成遮挡，一次画完。今日区间取自 Open-Meteo daily 下标 0（原本被整条丢弃），随 `current` 发布而不是进预报列表，预报列表仍从明天起算。证据：Release UnitTests 515/515（新增 `UT-WEA-024`/`025`/`076`）；`WorkspacePanel --smoke-test` 退出码 0；八条字形几何用 WPF `Geometry.Parse` + `FillContains` 逐点验过语法与填充（重叠处填实、缝隙处不填，无挖空）；安装后真实桌面点开面板，UIA 读到 `WeatherHighTemperatureText='28.9 °C'`、`WeatherLowTemperatureText='16.6 °C'`，截图确认插画平整无接缝、正文仍是卡片上对比度最高的元素。**未测**：深色与高对比度主题的实拍（两者只换调色板，高对比度按契约整组透明）；

- **设置对话框布局重排（2026-08-31，浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，实机逐栏截图复核）**。Before：三栏各有显示错误——天气栏「立即更新」按钮被裁掉右缘、常用地点列表被外层滚动区拦腰截断只露一行半；硬件监控栏内外两层滚动嵌套，内层列表把「CPU 频率」行裁一半，「网络来源」下拉与任务栏入口列表滚出视野且滚动条不可发现；Token 用量栏整段被误放在页脚 Grid 里，保存按钮直接叠画在说明文字上，说明文字右侧被裁，上方滚动主体卡渲染成一条空白圆角条。After：右侧改为单张卡面——唯一的滚动内容区（440 DIP 滚动道、416 DIP 内容宽，24 DIP 是滚动条专用道）加卡内钉住的「保存 + 行内状态」提交行；三个 section 全部位于内容区，硬件监控两张列表禁用自身滚动、随内容长高，整栏只有一条滚动条；位置名称与经纬度移入天气段正文，页脚三栏统一为按钮加状态。Why：**根因是 ContentDialog 的宽度由主题资源 `ContentDialogMaxWidth`（默认 548）钳制，控件上的 `MaxWidth` 不参与**，超出部分在卡面圆角处被静默裁掉——旧布局的按钮裁切与本轮英制开关加高后的列表截断都源于此；现已在对话框资源里显式覆盖为 672，并让所有停驻宽度与之共同变化。`WorkspacePanel --smoke-test` 退出码 0；三栏 After 截图经真实桌面生产路径复核，无一处裁切或叠画；

- **适配性整改三项（2026-08-31，产品原则 7）**：
  - 常用地点按面板语言取列表——中文取大陆加权列表，其余语言取世界城市列表（`UT-WEATHER-COMMON-001`～`004`，两份列表各自校验可保存、去重与降级）；
  - **Windows 11 之前不嵌入任务栏条带**：`MonitorSnapshot.taskbarStripTrusted` 由 `RtlGetVersion` 判定（build ≥ 22000），未信任时嵌入分支整体跳过、恒用工作区安全槽位；合成合同用例已入 `--geometry-smoke-test`，`--geometry-smoke-test` 与 `--entry-visual-smoke-test` 退出码均为 0。本机是 Win11，运行时行为不变；Win10 真机未测，属该路径的已知限制；
  - **天气显示单位公制/英制**：设置对话框新增开关，`weather.settings.*` 增加 `unitSystem`（迁移 8，旧行默认公制），provider 请求恒为公制、载荷带 `unitSystem` 标记，卡片投影与入口摘要在格式化时换算（`UT-WEA-074`/`075`、`UT-WEA-010`～`012`、`UT-WEA-073`）。Release UnitTests 512/512；`WorkspacePanel --smoke-test` 与 `CoreBroker --pipe-handshake-smoke-test` 退出码 0。**未跑**：真实桌面切换单位后的卡片与入口实拍，留待安装后日常使用验证；

- UI 评审固定条件：浅色主题、`3200×2000 @ 165 Hz`、200% DPI、中文、生产数据，基线提交 `10e7a2c`。Before：天气卡片把体感、湿度、风速排成三个裸数字（`32.4 °C` / `87%` / `1.6 km/h`，没有名称，第一个读作第二个温度），天气描述是硬编码英文（中文面板里显示 `Thunderstorm with hail`），观测时间是孤零零一行 `14:15`，署名是整张卡上最响的强调色链接，`L` 下半张卡是空的；设置浮层与便签独立窗口是系统默认的白底方框。After：次要读数横排三列、名称在上；描述按语言取字；观测时间与署名合成一行安静的页脚；内容按格数披露（见 UX 规范 3.4），`W`/`XL` 把预报并排放进空着的右半；设置浮层与便签窗口改用面板自己的材质、圆角与表面层次。Why：卡片的问题不是缺数据而是这些数字没说自己是什么。实测踩到一处：堆叠布局里预报块留在 `Auto` 列会被按无界宽度测量，日行的最高气温在星号列上被排到裁剪之外——**星期和字形显示，两个温度不显示**；补上 `Grid.ColumnSpan` 才对；
- **夜间天气插画的月牙路径是坏的**（本轮修复）：`M 52,6 A 44,44 0 1 0 52,94 A 34,34 0 1 1 52,6 Z` 的第二段弧半径 34 小于弦长的一半 44，渲染器把它放大成同半径半圆并与第一段抵消，成图几乎没有面积——夜间背景因此只剩三颗星。改为两个整圆按偶奇相减（外圆 r40 居中 50,50，内圆 r32 居中 57,50，内圆完全含于外圆故不留碎屑），已用 WPF `Geometry.Parse` + `FillContains` 逐点验证，实机截图确认月牙成形；
- Release UnitTests：566/566；
- Windows 自动天气位置真实桌面通过：正常冷启动只点击任务栏入口，系统位置访问时间更新，天气卡片显示“当前位置”；设置页开关、立即更新和 `ms-settings:privacy-location` 入口均可达；拒绝/无数据降级由单元测试覆盖，验收与 smoke 路径不请求位置；
- 参考机 `3200×2000 @ 165 Hz`、200% DPI：冷开 / 关闭 / 驻留重开的合成帧平均间隔分别为
  `6.03 / 5.98 / 6.04 ms`，最大间隔为 `7.30 / 6.45 / 8.93 ms`；120 ms 中途反向后保持常驻并正常隐藏，
  整段平均 `6.08 ms`、最大 `11.60 ms`。逐帧截图确认 6 张卡片使用不同的放射轴和朝向胶囊的边缘铰点；
  真实胶囊点击、关闭按钮点击和同 PID / 同 HWND 重开通过；
- CoreBroker 与 WorkspacePanel Release x64 构建：0 警告、0 错误；
- `WinWidgetBoard.CoreBroker.exe --pipe-handshake-smoke-test`、`WinWidgetBoard.WorkspacePanel.exe --smoke-test` 与 `--broker-smoke-test`（配真实 Broker）退出码均为 0；
- 真实桌面 `WinWidgetBoard.LauncherHost.exe --smoke-test` / `--panel-launch-smoke-test` / `--panel-lifecycle-smoke-test`（均配 `--no-broker`，指向新构建的 Release x64 面板）退出码均为 0：分层弹簧改造后，面板仍能展开、收起后在阀值内隐藏并保持常驻、再次展开为同一进程；
- 真实桌面 `Test-CardDragInteraction.ps1 -WithBroker` 通过（`REAL-DRAG-PASS`）：四种卡片的拖动柄、卡面、交互控件隔离和 `Esc` 取消经真实鼠标验证，握手、`cards.subscribe`、面板可见性上报和 `layout.save` 全部经真实命名管道走重构后的分发路径；
- **`Test-WeatherSettingsInteraction.ps1` 与 `Test-SystemMonitorInteraction.ps1` 目前不通过，且与本轮改动无关**——已用 `git stash` 回退到 `759b6ac` 和 `eba4d0d` 分别复跑，失败点完全相同。设置流程本身通过：搜索 Tokyo、选中候选、填入只读经纬度、保存并等到 `StatusText` 出现「天气位置已保存」全部走通；卡住的是其后「卡片的位置文字变为 Tokyo」这一步（30 s 内不出现，把等待放宽到 150 s 时通过一次，120 s 时又失败，属于偶发）。已定位到的事实：Broker 在切换位置时确实发布了带新 `location.label` 的 Loading 快照（`WeatherProviderRuntime.ApplyRegistrationLocked`），且新旧注册共用同一个序号计数器，因此不是序号被判旧；而 `WeatherCardProjection` 当时在读不到 `location.label` 时回退到硬编码英文字面量 `"Weather"`，那既是卡片显示的内容，也违反「用户可见字符串只放在 resw」——该回退已改为空串（没有地点就不画那一行），天气描述也已改为由投影给出资源键、卡片按语言取字。硬件监控脚本卡在更早的「卡片出现百分比读数」一步，同样在两个历史提交上复现。两者都留待天气阶段一并处理；
- CI（`windows-2025-vs2026` 托管镜像）Debug 与 Release 双配置全绿：整解决方案 `msbuild` 构建、全部单测、LauncherHost 与 WorkspacePanel 的 `--smoke-test` 全部通过；
- **首次启动时 provider 卡片一直「正在加载」已修复**（`COLDSTART-PASS`）。成因：`CardSubscriptionLifecycleCoordinator.RequestRefresh` 在尚未初始化时直接返回，而冷启动路径上面板变可见的时刻早于首次订阅完成，那次请求被丢弃——订阅因此带着「布局之前的视口」生效，即一张卡片都没有，天气与硬件就停在加载态，直到下一次打开才重新请求。现在把该请求记下并在初始化完成后补发（并 marshal 回 UI 线程，`DispatcherQueueTimer` 只接受 UI 线程）。实机 A/B：不打补丁时首次打开 90 秒内两张卡片都不离开加载态；打补丁后 **1 秒**内就绪；
- **动效帧率已定位并修复**。折页的几何原先逐帧写 6 张卡片的 `CompositeTransform`，这就是全部代价。逐项隔离测量（隔离面板，`3200×2000 @ 165 Hz`、200% DPI，帧预算 `6.06 ms`）：

  | 配置 | 展开均值 | 收起均值 |
  | --- | --- | --- |
  | 原实现 | `12.19 ms` | `15.69 ms` |
  | 完全不应用折页 | `6.00 ms` | `6.05 ms` |
  | 去掉位图缓存 | `12.20 ms` | `16.22 ms` |
  | 只写变换、不写透明度 | `11.95 ms` | `16.23 ms` |
  | 只写透明度、不写变换 | `5.98 ms` | `5.94 ms` |
  | **改到合成视觉后** | **`6.09 ms`** | **`6.27 ms`** |

  结论：透明度免费，位图缓存毫无作用（已移除），代价全在 `CompositeTransform` 上。位移用
  `UIElement.Translation` 而非视觉的 `Offset`——**第一版写了 `Offset`，把所有卡片摞到了左上角**：
  `Offset` 是布局给元素定的位置，写它是替换不是叠加。改用 `Translation` 后帧间隔为 `6.05 / 5.95 ms`，
  且最大值由 `12.57 / 16.15` 收紧到 `7.42 / 6.31`。几何改写到卡片自身的合成视觉后回到刷新率，且帧数由 48/30 增至 96/74——同样时长里帧数翻倍。显示器经 `Win32_VideoController` 确认当前就是 `165 Hz`，不是显示设置问题。
- **折页命中一致性已实机验证**：新增 `scripts/Test-CardFoldHitTest.ps1`，4 轮（共 8 次折页）× 6 个跨网格探针，`REAL-CARD-FOLD-HITTEST-PASS`。它在折页settle后取每个探针**绘制中心点**，问系统该点上是谁，要求答案属于同一张卡片——这正是合成层折页唯一的真实风险：某次复位漏掉，面板画对了而输入落在别处。按几何重叠比对而非遍历树，因为要证的是"系统在那里找到的是这块面板"，不是 UIA 返回哪一层。
- **拖动类脚本只能由使用者跑，代理跑不了**（2026-08-31 已由使用者跑通 `-WithBroker` 与 `-ResizeDragCombination` 两种模式）。代理侧四次尝试都止步于脚本自身的前置检查，原因已查清：脚本自启的面板抬不到最前——Windows 拒绝把前台交给一个本身不在前台的进程（控制台宿主永远不是），因此桌面上任何最大化窗口都会压住它。证据：报出的遮挡进程随当前开着的窗口变化（`REDAgent` → `claude` → 把 `claude` 最小化后变成 Progman 桌面），而面板几何与任务栏并不重叠（面板底边 `1884`，任务栏 `1904` 起）。**由使用者在自己的交互式终端里运行即可通过**——那时控制台就是前台进程，已实测如此。已尝试在共享模块里用一次真实点击换取前台权限，未能解决，改动已回退，不留未验证的脚本改动。注意这条只挡「脚本自启面板后请求前台」这一条路径：代理仍可用合成点击激活任务栏胶囊，再注入拖拽、读 UIA、截图，本轮缩放手势的实机证据就是这么取的。另一条前置检查「已有 LauncherHost 在跑」只是安装版常驻，退出后即可，不构成限制；
- 真实桌面便签独立窗口通过（`NOTEWINDOW-PASS`）：窗口开在 `840×920` 物理像素（= `420×460` DIP），勾选「保持在最前」后 `WS_EX_TOPMOST` 由 False 变 True，弹出窗口打开即显示卡片当时的正文（证明两处共用同一个编辑器而非各持一份），在窗口里输入后状态行报「已保存到本地」。**注意**：面板在焦点移到弹出窗口时会隐藏自己，所以「卡片是否跟随」无法在窗口置前时读取——改为断言弹出窗口打开时与卡片内容一致，方向等价且不受焦点影响；
- 真实桌面便签四脚本全部通过（2026-09-02 复跑）：`REAL-NOTE-LIST-PASS`、`REAL-NOTE-DELETE-PASS current+list`、`REAL-NOTE-MARKDOWN-PASS mode+preview+roundtrip`、`REAL-NOTE-CREATE-PASS create+list+search+open+flush-before-switch+toggle`（原 `draft-guard` 段改为断言「刚输入的草稿先落库、再切到所选便签」，并加了列表按钮开合各一次）。首次迁入卡片时修掉两处由改动暴露的问题：滚动内容现在恰好等于视口高度，`ScrollPattern.SetScrollPercent` 会在「可滚动」检查与调用之间翻转并抛 `InvalidOperationException`（模块里改为忽略该状态——没有可滚的东西正是调用方要的结果）；`Test-NoteCreateInteraction.ps1` 需要在选中便签后重新打开切换器才能再用搜索框；
- 真实桌面天气卡片（生产路径，经启动器打开面板，安装后的正式构建）：`L` 下一屏显示北京 `26.2 °C`、字形 + **`雷阵雨伴冰雹`**（此前是 `Thunderstorm with hail`）、`体感 32.2 °C / 湿度 89% / 风速 1.3 km/h` 三列带名称、`15:00`–`20:00` 六格走势、`周三/周四/周五` 三行含最高与最低、页脚 `更新于 14:30 · Weather data by Open-Meteo.com`，**无一处被裁**。逐尺寸实拍（编辑模式下经 UIA 逐级改尺寸）：`S` 只剩地点 + 气温 + 天气；`M` 加次要读数；`W` 走势并排在右半，十格 `15:00`–`00:00`；`XL` 同样并排。**编辑模式下的注记**：编辑工具条本身占一行，`M`/`W` 在编辑模式里会把次要读数挤掉——非编辑态高度够，这是工具条的代价不是披露规则的问题；
- 每张卡片的放大/缩小按钮补上了各自的 `AutomationId`（`WeatherCardIncreaseButton` 等）。此前它们只有共享的自动化名称，一次逐尺寸取证脚本因此按到了别的卡片的按钮，把板子的顺序改掉了——脚本无法说清自己要按的是哪一张卡；
- **未测**：`Test-SystemMonitorInteraction.ps1` 与 `Test-WeatherSettingsInteraction.ps1` 的后半段在隔离栈里仍不可用——直接以 `--acceptance-test` 启动的面板不上报可见性，两张 provider 卡片在该路径下长期停留在「正在加载」。这不是本轮引入的，回退到 `759b6ac`、`eba4d0d` 复跑失败点相同，留待单独排查；
- 真实桌面设置浮层与便签独立窗口（生产路径实拍）：浮层为半透明面板材质、16 DIP 外圆角（由 `OverlayCornerRadius` 而非控件 `CornerRadius` 决定）、分类栏与两个分段各坐在自己的表面上、常用地点列表带边界并完整显示五项；关闭按钮由强调色实心条改为普通按钮。便签窗口为 Desktop Acrylic + 强调色薄雾 + 面板头部条，正文坐在卡片表面上，「保持在最前」为图标切换按钮，系统标题按钮由 `RightInset` 换算出的空位让开。**未做到**：`ContentDialog` 模板给命令按钮写死了 `HorizontalAlignment="Stretch"`（模板里的本地值压过样式 Setter），所以关闭按钮仍然通栏；试过的右对齐样式无效，已删掉而不是留一条证明无效的 Setter；
- 真实桌面网络来源全链路通过（隔离 Broker + 面板，`NETSOURCE-PASS`）：下拉列出「全部网卡（合计）」加本机 4 块网卡，选中「以太网」保存后关闭再开，选择由 SQLite 读回一致。**未测**：没有在可控流量下比对「选定网卡」与「全部合计」的实际速率差异，过滤本身只是一次 ID 相等比较，且与列表枚举共用同一段代码；
- 真实桌面 `Test-LauncherEntryPlacement.ps1` 通过：入口落在任务栏条带 1904..2000 内，
  入口外的条带点位归属其他进程（穿透成立），按 Explorer 的方式重排任务栏 5 次入口 0 次被盖住
  （`ENTRY-ZORDER-PASS`），点击开启面板；
  `--geometry-smoke-test` 与 `--entry-visual-smoke-test` 退出码均为 0。
  该脚本必须在 DPI 感知的宿主（`pwsh`）里运行：Windows PowerShell 5.1 是 DPI 非感知进程，
  `GetWindowRect` 会返回虚拟化后的坐标，200% 缩放下把宽度报成一半并误判为越界；
- 真实桌面：Broker 与入口同时运行、无面板打开时，入口显示真实读数（Open-Meteo 真实请求），
  实测安装态入口为 `16,1912 - 240,1992`（112×40 DIP，DPI 192），晴天配色与日轮图形已截图核对；
  同一构建以 `--entry-icon-preview` 输出 192 DPI 对照表，十一种天气 × 深/浅条带逐格评审；
- **`Test-SystemMonitorInteraction.ps1` 目前不通过，卡在「切换 `cpu.clock` 后卡片跟随」这一步**
  （2026-09-01 复跑两次，失败点相同）。已定位：隔离栈里 provider 的刷新推不动——UIA 全量名称快照显示
  卡片上 `CPU`、`GPU`、`下载`、`上传` 四条全停在「测量中…」（只有不需要基线的内存有读数），
  也就是首次采样之后再没有新快照，因而新启用的指标不会出现在卡片上。这与
  §「未测」一条记的是同一个成因（`--acceptance-test` 直接启动的面板不上报可见性），
  不是本轮改动引入的。脚本本身已补上温度一段（换入 `cpu.temperature`、换出 `cpu.clock`，行数不变），
  等该成因修好后一并生效；
- **传感器源文案已在生产路径实机验证**（2026-09-01，安装后构建，经任务栏入口打开面板）：
  硬件卡片经 UIA 播报 `CPU 温度 需 HWiNFO/Core Temp`（多来源改动后复测，文案未被裁切），
  同一张卡上 `CPU 19%`、`内存 53% 16.7 GB / 31.4 GB`、`GPU 52%`、`CPU 频率 4.44 GHz`、
  `上传 855 B/s`、`下载 2.6 KB/s` 均为真实读数。
  参考机既没有 HWiNFO 也没有 Core Temp，**因此有传感器源时的读数与两个真实块的解析都尚未在真机上验证过**；
- 真实桌面：安装态入口同时显示主胶囊与硬件胶囊，实测窗口 `16,1912 - 656,1992`（320×40 DIP，
  DPI 192），其中主胶囊为时钟、硬件胶囊为 CPU/MEM 与上下行两列；`--entry-icon-preview` 的
  192 DPI 对照表新增「单独硬件胶囊」与「主胶囊 + 硬件胶囊」两组，逐格评审字号与走势图；
- 真实桌面（`--no-broker`，因此没有读数、只画主胶囊）实测 `16,1912 - 360,1992`，172×40 DIP，
  监视器 3200×2000、工作区高 1904，入口整体落在 1904..2000 的任务栏条带内，上下各留 8 DIP。
  宽度随主胶囊内容变化，不由几何决定。
  **未跑**：`Test-LauncherEntryPlacement.ps1` 本身在本机已无法启动——它要求进程达到
  per-monitor DPI 感知，而 PowerShell 7 在进程创建时已被设为 system 感知（`GetDpiForSystem`
  与显示器 DPI 同为 192），`SetProcessDpiAwarenessContext` 只能设一次，返回 `ERROR_ACCESS_DENIED`。
  上面的矩形是在确认「单显示器且显示器 DPI == 系统 DPI，因此坐标不被虚拟化」之后直接量的；
- 真实桌面 `Test-AddCardInteraction.ps1` 通过（`REAL-ADDCARD-PASS`）：编辑模式外添加按钮不可见，
  进入编辑后出现；全部卡片在板时不弹空对话框；移除计时器卡片后经选择器加回，完成编辑并重启面板后
  仍在布局中；
- `WinWidgetBoard.CoreBroker.exe --sysmon-smoke-test` 退出码 0，参考机上 9 项读数为 `ready`
  （空载时 `cpu.clock` 为 3.62 GHz，12 线程满载时为 4.59 GHz，同刻独立复核为 4.50 GHz，
  该型号 P-core 睿频上限 4.9 GHz），CPU 温度、GPU 温度、风扇 3 项为 `unavailable`；网速采样改为仅累计具有 IPv4/IPv6 接口索引的
  IP 层端点，参考机从 17 个原始 `Up` 条目收敛为 4 个逻辑接口；`UT-SYSMON-054/055` 覆盖接口
  加入、移除和历史累计值不得形成尖峰；
- 验收使用隔离临时数据和独立实例身份，结束后已清理。

这属于 L2 针对性证据，不替代发布候选的完整显示器、偏好和无障碍矩阵。更早轮次的完成证据以 Git 提交和测试名称为准。

### 4.1 参考机启动与后台占用

测量条件：Intel Core Ultra 5 225H（14 逻辑核）、31.4 GB 内存、Windows 11 25H2 build 26200.9168（注册表 `ProductName` 仍显示 `Windows 10 Pro`）、Release x64、未附加调试器、基线提交 `eb07d3e`、工具 `scripts/Measure-StartupFootprint.ps1`、每阶段 5 次迭代、空闲采样 60.9 秒、隔离临时数据目录。

| 指标 | 最小 | 中位 | 最大 |
| --- | --- | --- | --- |
| 面板进程启动 → 首个窗口 | 661.2 ms | 669.3 ms | 673.6 ms |
| 面板进程启动 → 布局就绪状态 | 872.2 ms | 880.0 ms | 928.1 ms |
| 任务栏入口点击 → 面板窗口 | 678.9 ms | 695.6 ms | 720.0 ms |

无面板打开时的空闲占用（后台天气启用**前**）：CoreBroker 在 60.9 秒内消耗 109 ms CPU（全核 0.013%），工作集 43.1～46.4 MB，中位 43.9 MB；LauncherHost 消耗 0 ms CPU，工作集恒为 12.1 MB。

启用每小时后台天气刷新**后**，同机 90 秒空闲采样：CoreBroker 消耗 422 ms CPU（全核 0.033%），工作集 52.0～56.8 MB，中位 52.8 MB；LauncherHost 消耗 78 ms CPU（全核 0.006%），工作集 11.6 MB。CoreBroker 的 CPU 包含采样窗口内那一次启动刷新，不代表每小时稳态。

### 4.2 面板冷启动耗时构成与常驻结果

用 `WorkspacePanel.exe --startup-trace`（默认关闭，只写 stderr）在同一次运行内分段。**跨运行的绝对值不可比**——本会话后期机器负载明显升高，同一构建的 `layout-ready` 从约 980 ms 漂到约 1730 ms；下表取同一次运行内的分段与占比。

| 阶段 | 占比 | 说明 |
| --- | --- | --- |
| 进程 + .NET 运行时 + WinAppSDK 启动 | 约 25～30% | 我们的第一行代码之前 |
| `App` 构造与 XAML 框架初始化 | 约 5～7% | |
| `MainWindow` 的 `InitializeComponent()` | 约 28～30% | 单个 62 KB XAML |
| `MainWindow` 构造函数后半段 | 约 22～25% | 其中约 105 ms 是 ViewModel 构造，约 170 ms 是窗口互操作与几何：`ConfigureToolWindow`、`AppWindow` 取用、`PanelGeometry.Calculate`、`ApplyPlacement` |
| 激活 + Broker 往返 + 布局绑定 | 约 10～13% | |

结论：耗时集中在运行时/框架初始化、单个大 XAML 的展开，以及**必须在显示前完成**的窗口与几何设置。构造函数后半段那 170 ms 不可延迟——推迟只会把代价挪到激活时。因此「延迟加载 + ReadyToRun」这条低成本路径**没有明显可摘的果实**。

`PublishReadyToRun` 未采用：`dotnet publish` 产物不含 `resources.pri`，`ResourceLoader` 直接抛 `FileNotFoundException`（退出码 20）；手工补齐 pri 后的测量受目录与环境差异污染，不构成有效对比。要走这条路必须先修好发布配置，属于独立工作。

已按 [ADR-0025](../adr/0025-resident-workspace-panel.md) 实施常驻：关闭隐藏窗口并保留进程，隐藏时调用 `EmptyWorkingSet` 交还页面。实测结果：

| 指标 | 常驻前 | 常驻后 |
| --- | --- | --- |
| 入口点击到面板可见 | 约 740 ms | 27～42 ms（含 UIA 轮询，属上界；裸窗口可见性测得 9～17 ms） |
| 面板工作集（隐藏时） | 进程已退出 | 18～37 MB |
| 面板工作集（显示时） | 178～249 MB 峰值 | 60～66 MB |
| 面板空闲 CPU | 进程已退出 | 94 ms/30 秒（全核 0.022%） |

启动器驱动的 8 次连续开关全部符合预期。常驻同时暴露并修复了一个状态漂移：面板会因失焦自行隐藏而不通知启动器，而旧的复位路径依赖进程退出；启动器现在以面板窗口的真实可见性为准。

这是单台参考机的一次测量，不覆盖多显示器、DPI 缩放、低配设备和长稳；发布候选的性能矩阵仍未执行。上表的延迟数字取自后台天气启用前的那次运行；之后的复测在机器负载明显更高时进行，与之不可直接比较，故未替换。

## 5. 当前技术债务

1. `MainWindow.xaml.cs` 仍同时协调便签删除/编辑、拖动和设置；卡片订阅、布局持久化、便签列表和动效已提取。
2. `CoreBrokerCommandRouter.cs` 只保留 `session.ping`、方法分发和连接释放；便签、布局、天气设置、卡片订阅和面板可见性各自成为领域 handler，共用同一把锁与同一份操作缓存上限。
3. `ProviderRefreshScheduler.cs` 单文件状态机过大。
4. `EntryVisual.cpp`（约 3600 行，仓库最大单文件）同时承担自绘、动效、两枚胶囊排版、天气插画和走势图；与 `MainWindow.xaml.cs` 同一策略——下次真要改其行为时先按职责拆，不单独开一轮。
5. UIA 公共窗口、元素、输入和进程辅助函数已提取到 `scripts/WinWidgetBoard.UiAutomation.psm1`；查找一律限定在目标进程自己的顶层窗口内，并对可重试的 UIA COM 故障退避重试。
6. Windows App SDK 自包含输出较大，开发构建不应长期留在仓库。
7. 远程仓库 `CookPiu/WinWidgetBoard`（私有）已配置。CI 在 GitHub 托管镜像 `windows-2025-vs2026` 上 Debug 与 Release 双配置全绿，单个 job 约 2 分钟，已按 `push` / `pull_request` 自动触发，纯 Markdown 改动不触发。该镜像自带 VS Enterprise 2026 `18.8.12023.21`、Windows SDK `10.0.26100.0`、.NET SDK `10.0.302` 和 `VC.14.44.17.14.x86.x64` 工具集，项目锁定的 `VCToolsVersion 14.44.35207` 解析正常，无需放宽任何锁定值。
8. 整解决方案构建已在 CI 上验证通过，但仍无法在本机进行：本机 VS MSBuild 解析不到 `Microsoft.NET.Sdk`，设置 `MSBuildSDKsPath` 也只能多走一步，随后卡在 `Microsoft.NET.SDK.WorkloadAutoImportPropsLocator`。本地仍按分项目构建。
9. 完整显示、无障碍、性能和发布矩阵尚未执行。
10. **入口已改为任务栏窗口的 owned window**（`GWLP_HWNDPARENT`），不再与任务栏争 topmost。
   成因：Explorer 在处理窗口激活时会把 `Shell_TrayWnd` 重新提到 topmost 顶端，短暂盖住入口，
   表现为「从任务栏打开最大化窗口时胶囊闪一下」；不是可见性也不是重定位，实测入口始终
   `IsWindowVisible=True`、矩形不变，只是 z-order 被压下去。
   `EVENT_SYSTEM_FOREGROUND` 上重置 topmost 加 40 ms 三连补刀不足以消除：参考机实测单次覆盖
   47–393 ms，Explorer 在 120 ms 补刀窗口之后重排时只能等 500 ms 轮询兜底。owned 窗口恒在 owner
   之上，从结构上退出这场竞争；只按类名取任务栏顶层窗口，解析不到时降级回原重置路径，
   `EVENT_SYSTEM_FOREGROUND` hook 与 500 ms 轮询都保留。
   参考机实测（单显示器，200% 缩放，按 Explorer 的方式重排任务栏 5 次）：改动前 5/5 被盖住，
   改动后 **0/5**；`Test-LauncherEntryPlacement.ps1` 连续 5 轮全绿（新增 `ENTRY-ZORDER-PASS`）。
   Explorer 重启：跨进程 owner 被销毁只会把 owner 清零而不销毁 owned 窗口，入口窗口存活，
   `TaskbarCreated` 上重新挂到新任务栏——实测 owner 由 `65934` 更新为 `6557024`，重排后仍 0/5。
   多显示器（`Shell_SecondaryTrayWnd`，按入口所在监视器匹配）已实现但本机只有单显示器，未实测。
   其他 topmost 第三方任务栏扩展仍与入口共享层级，真实回归仍须先等待入口占据自身中心点再点击。
11. 计时器、待办和日历占位卡已退出面板：不在默认布局里、不在添加选择器里，旧布局回放时静默丢弃；卡片定义与模板仍保留（不扩展），能力本身仍按 ADR-0022 延期。
12. **入口的 UI 线程不再向 Explorer 发同步消息**。此前 `CaptureMonitorSnapshot` 调用
    `SHAppBarMessage(ABM_GETSTATE / ABM_GETTASKBARPOS)`，两者都是向 `Shell_TrayWnd` 发的
    无超时 `SendMessage`。实测两次事故（2026-08-21 10:10:16、2026-08-29 10:23:34）都是
    `AppHangB1` + `ConsentKey=AppHangXProcB1`（等待链跨进程），且都与 Claude 桌面版 MSIX
    servicing 同秒发生（`AppXDeploymentServer` 2562 + 服务重装）：servicing 广播
    `WM_SETTINGCHANGE` → 入口 `ScheduleReposition()` → `SHAppBarMessage` 卡在忙于同一次
    servicing 的 Explorer 上 → 入口停止泵消息 → Windows 判定无响应并结束进程 → job object
    连带带走面板与 Broker（用户观感是「面板自己消失」）。
    两个读数都可以不要：`ABM_GETTASKBARPOS` 只报主任务栏且结果从未被读取；`ABM_GETSTATE`
    的自动隐藏标志已被几何蕴含——自动隐藏只留下比 `kMinimumTaskbarThicknessPx`(8px) 更薄的
    唤出带，`DeriveTaskbarStrip` 与 `IsSingleEdgeDifference` 本就会拒绝它。唯一未覆盖的组合
    是自动隐藏叠加另一个应用恰好占用单侧工作区，那与该侧存在任务栏在几何上不可区分。

## 6. 当前工作

本轮已完成（硬件监控，[ADR-0028](../adr/0028-system-monitor-scope-and-sensor-tiers.md)）：

- **范围变更**：核心五项变为核心六项。`ADR-0022` 将系统监控列为延期项并要求明确再批准，
  本轮即该再批准的落地；
- **公开 API 层**覆盖 CPU 占用与频率、内存、GPU 占用与显存、磁盘活动与占用、网络上下行共 9 项，
  全部走 `GetSystemTimes` / `GlobalMemoryStatusEx` / `NetworkInterface` / `DriveInfo` / PDH，
  无新依赖、无驱动、无管理员；PDH 一律用 `PdhAddEnglishCounter`，否则在中文 Windows 上静默失效；
- **CPU 频率由每核 PDH 计数器测出**（[ADR-0031](../adr/0031-cpu-clock-from-per-core-counters.md)）：
  `\Processor Information(*)\Processor Frequency` 是**每颗核的标称频率**（参考机 P-core 1700、
  E-core 1300、LPE-core 700 MHz），只有 `_Total` 汇总实例跨核类求平均才恒为 1.33 GHz；
  与同实例的 `% Processor Performance`（APERF/MPERF 比值）相乘即该核的实际平均频率，
  取最快的一颗上报。超过 10 GHz 的结果整条丢弃，首拍报 `pending`；不画进度条也不画走势图，
  因为睿频上限在用户态读不到。`CallNtPowerInformation` 与 `Win32_Processor.CurrentClockSpeed`
  在本机满载与空载都恒等于标称值，已排除；
- **温度与风扇报 `unavailable`**，读取需要内核驱动，本产品不分发。本轮复核：
  `MSAcpi_ThermalZoneTemperature` 普通用户仍拒绝访问，
  `Win32_PerfFormattedData_Counters_ThermalZoneInformation` 只有一个恒 0 的芯片组温区，
  `Win32_Fan` 不提供转速；
- **采样按需**：卡片可见或入口近 10 秒内索要过摘要才以 2 秒节奏采样，否则完全休眠，
  唤醒时丢弃增量基线；
- **网速去重与尖峰保护**：不再把所有 `Up` 的 `NetworkInterface` 累加。WFP、QoS 与抓包过滤层
  可能重报实体网卡的同一字节计数，却没有可用的 IPv4/IPv6 接口索引；现在仅统计 IP 层端点。
  基线按接口 ID 保存，接口加入、移除、重连或计数器回退时下一拍报 `pending`，随后再建立速率，
  不把接口的历史累计值误作当前 2 秒流量；
- **卡片与任务栏各自独立配置**，每处最多 8 项，顺序即列表顺序，经版本化 IPC 落盘；
- **入口是两枚胶囊**：左边主胶囊（指示器 + 时间或天气，96～280 DIP），间隙 8 DIP，
  右边硬件胶囊（上限 440 DIP，无下限，没有读数时整枚不画）。硬件监控因此是独立开关而非
  第三种显示内容，天气与读数可以同时存在；旧的 `Content = 2` 配置在读取时迁移成
  「时钟 + 显示硬件监控」；
- **硬件胶囊两行密排**：两个读数为一列，列间 1 DIP 细线跨越两行；分段字号 10 DIP、图形 12 DIP；
  装不下的**整列**省略而不半截截断，落单读数居中；条带放不下两枚时先丢硬件胶囊而不是入口本身；
  入口不出现任何需要翻译的文字；
- **宽度滞回改为非对称**：原来只要测得宽度变化不足 8 DIP 就不重排，内容却可能已经超出预留宽度；
  胶囊放不下时不截断而是整列丢弃，于是**默认配置下最后一列（网速）会间歇性消失**。
  参考机复刻测量：CPU/MEM 固定、网速文本让整条在 136～157 DIP 之间摆动（21 DIP），
  例如已预留 152 DIP 时测得 154 DIP，差 2 DIP 落在滞回带内 → 网速列被丢。
  现在增长只要装不下就立刻重排，收缩要等空余累积到 16 DIP 才回收，每次重排多留 8 DIP 余量；
  实测 70 秒变速下载期间入口宽度 0 次变动、网速列全程在位；
- **字体按名字取 Semibold**：向 `Segoe UI` 家族请求 `FW_SEMIBOLD` 在本机解析成 `tmWeight 700`
  （GDI 字体映射向上取到 Bold 面），这个尺寸下全粗体会把字腔填死、边缘发毛；改为直接指定
  `Segoe UI Semibold` face 后解析为 600。同时文本蒙版改用 ClearType 渲染但按三个子像素样本
  **取平均**而非取最大值——等于水平方向三倍采样的灰度覆盖率，且因为最终只用一个颜色合成，
  不可能产生彩边。实测同一区域：满覆盖像素 545 → 490（笔画变轻），边缘半覆盖像素 598 → 735；
- **每个读数画在自己的历史走势之上**：最近 60 个采样（2 秒节奏 ≈ 2 分钟）由 Broker 归一化为
  0..1 随 `sysmon.summary.get` 下发，入口只把它映射成一层填充面积。有上限的指标按自身上限绘制，
  速率按窗口峰值相对绘制并设 4 KB/s 下限门槛，读不到的指标不画图。

**传感器层已验证并放弃**（[ADR-0029](../adr/0029-drop-the-bundled-sensor-driver.md)）：
以管理员运行 LibreHardwareMonitorLib 0.9.6，CPU 温度、CPU 频率、风扇**全部无读数**，
该次运行未安装任何服务——提权不是瓶颈，驱动本身没有加载。唯一多出来的是硬盘温度 45 °C
（NVMe SMART，需管理员）。同机 `R0TrafficMonitor` 与 `RTCore64` 均在运行，说明本机允许内核驱动加载，
被拦下的是该库分发的那个具体驱动。

因此不引入第三方运行时依赖、不新增第四个进程、不分发任何 WinRing0 系驱动（含改名变体）——**这三条至今成立**。
CPU 频率并不需要驱动，已按 [ADR-0031](../adr/0031-cpu-clock-from-per-core-counters.md) 改为可读；
温度与风扇已按 [ADR-0033](../adr/0033-hwinfo-shared-memory-sensor-source.md) 与
[ADR-0034](../adr/0034-core-temp-as-a-second-sensor-source.md) 改为读取监控程序的共享内存
（HWiNFO 三项、Core Temp 仅 CPU 温度），没有来源时该行不显示（来源要求写在硬件监控设置页），源在运行而确实读不到时仍显示「此电脑无法读取」。

**四条免驱动替代路径已在参考机上逐条验证，全部为否**（2026-09-01，只读探测，未安装任何东西）。
记在这里是为了不再重试：

| 路径 | 结果 |
| --- | --- |
| ACPI 热区（`Win32_PerfFormattedData_Counters_ThermalZoneInformation` / `MSAcpi_ThermalZoneTemperature`） | 热区 `\_TZ.TZ00` 存在，但 `CurrentTemperature` 恒为 0（提权后仍是 0，即 −273.2 °C），只有临界点 383.2 K 是真的 |
| 联想厂商 WMI（`LENOVO_GAMEZONE_DATA`） | 实例存在（`ACPI\PNP0C14\GMZN_0`），但 `GetCPUTemp`／`GetGPUTemp`／连 `GetVersion` 在内的**每一个方法**都返回「无效的对象」；CIM 与旧版 WMI 两种调用形态、提权与非提权都试过。类是驱动注册的，固件没有实现 |
| 联想风扇 WMI（`LENOVO_FAN_METHOD` / `LENOVO_FAN_TEST_DATA`） | 只有 `Fan_Get_Table` / `Fan_Set_Table` 与一张静态的最大／最小转速表（两把风扇 4300/4600 与 1700/1200 RPM），**没有当前转速** |
| MSI Afterburner 共享内存（`MAHMSharedMemory`，本机常驻） | 非提权可读、布局解析正确（`MAHM`／`0x00020000`／entry 1324 B），9 条读数全是 GPU 占用、显存、核心频率、功耗、CPU 占用、内存、帧率——**没有任何温度或转速**（与 [ADR-0029](../adr/0029-drop-the-bundled-sensor-driver.md) 当时的观察一致） |

即：这台机器上除了内核驱动，没有第二条通往 CPU 温度的路。TrafficMonitor 之所以能显示温度，
靠的正是它的「硬件监控插件」加载 LibreHardwareMonitor 释放的 WinRing0 系驱动（服务 `R0TrafficMonitor`、
文件 `TrafficMonitor.sys`），并因此要求管理员运行——那是 [ADR-0029](../adr/0029-drop-the-bundled-sensor-driver.md)
第 2 款明确拒绝分发的东西，不是本产品可以照搬的部分。本机现存的两个同族驱动
（联想 PCManager 的 `COOLit64.sys` 占着 `WinRing0_1_2_0` 服务名、MSI Afterburner 的 `RTCore64`）
同样不在可利用范围内：调用别人装的提权驱动比自己分发一个更糟。

**自签一个窄接口内核驱动这条路已评估并放弃（2026-09-01，用户决定）。** 技术上它成立且不同于
WinRing0——可以只读、只开放白名单化的几个 MSR、设备对象带 ACL、不提供任何写入口，因此不构成
通用提权原语，也就没有被驱动黑名单的理由。放弃的原因是代价：EV/OV 代码签名证书加微软
attestation 签名的持续费用与实名流程、驱动本身在 HVCI 与后续 Windows 版本上的长期维护，
以及一个对开源项目更麻烦的问题——**发布包里的驱动无法从源码复现**，自行编译的用户拿不到签名驱动。
为一项读数付这些，不划算。

因此**温度与风扇的能力边界就到此为止**：用户已在运行 HWiNFO 或 Core Temp 时显示真实读数，
否则显示需要哪一个。这不是待办，是结论；除非出现新的免驱动路径（例如独立显卡经厂商 API
读 GPU 温度），否则不要重新打开这个题目。

上一轮已完成（天气三项优化）：

- **入口改为图标 + 温度，去掉地区**：地区是用户自己设的，每分钟重复一遍只是在花掉入口宽度。
  Provider 增加 `is_day`，并把 WMO 码归约成 `conditionIconId` 标记放进 Contracts，
  由 Broker、面板卡片和原生入口共用一张表；入口用自己的合成器画出十一个图形
  （圆、圆角矩形和一个多边形），不引入图标字体，单色跟随文字颜色；
- **卡片获得随天气变化的平面插画**：底色 + 一到三个图形放在卡片右下角空白处并溢出圆角，
  裁剪到卡片自身圆角，不接收指针输入、不带自动化名称；配色集中在 `WwbWeather*` 主题画刷，
  高对比度全部清零。`UT-UI-002` 由「禁止一切字面颜色」收紧为「字面颜色只能出现在天气插画画刷上，
  且必须在高对比度里被清零」——是加强而不是放宽；
- **位置设置改为地点搜索**（[ADR-0027](../adr/0027-weather-location-search.md)）：
  新增 `weather.locations.search`，经 Open-Meteo 地理编码端点解析地名，返回最多 8 条候选；
  对话框改为「搜索 → 选择 → 保存」，经纬度只读展示；搜索只在提交时发生，不做按键即搜。
  这是第一条把用户输入文本送出本机的路径，隐私文档已相应更新；
- 该方法是唯一的异步命令，单独在管道循环里 await，不占用路由器全局锁；
- **入口获得与卡片同源的天气插画**：整枚胶囊铺天空色，尾部预留 26 DIP 画当前天气的图形，
  越过收尾圆头少许并被胶囊裁切。配色是 `WwbWeather*` 的重述（入口是分层窗口，没有
  `ThemeResource`），`WorkspaceVisualStyles.xaml` 仍为权威；`unknown` 与非天气模式不画也不预留，
  高对比度完全不画。插画画在中性底色**之上**——画在其下时天空与图形都会被冲淡成一团灰；
- 共享 UIA 模块增加 `Save-WindowCapture`：用 `PrintWindow` 抓取窗口自身内容，
  不再受 z 序与焦点影响——常驻面板会因失焦隐藏，屏幕区域截图因此并不可靠。

修复了一个真实缺陷：**卡片一直丢弃 Broker 推送的快照**。
`sequence` 有两个互不相干的计数器 —— Broker 的，以及面板自己
（占位快照、可见性调度）的 —— 而 `ApplySnapshot` 把两者放在同一个
字段上比较。面板先跑一会儿后本地序号就超前了，之后 Broker
推送的真实天气全部被当作「更旧」丢弃，卡片停在占位态。
现在 Broker 快照只在它自己的序列空间内比较，首条无条件生效
（`UT-CARD-097`～`UT-CARD-099`）。`Test-WeatherProviderInteraction.ps1` 因此由失败转为
`REAL-WEATHER-PASS temperature="29.9 °C"`。

本轮修正的一处判断错误：先前认为「天气卡片收不到数据」是既有缺陷，实际上是我一直在运行
**前一天的面板产物**。`Install-WinWidgetBoard.ps1` 与真实桌面脚本读取的是 `x64\Release`，
而 `dotnet build -c Release` 不带 `-p:Platform=x64` 写的是另一个路径。加上 `-p:Platform=x64`
后卡片订阅正常：`cards.subscribe` 返回 `ready` 快照并被应用。该陷阱已写入 CLAUDE.md。

## 7. 下一步

七项核心能力已全部具备真实桌面证据，远程与 CI 已建立。当前优先级由「继续改代码」转为「靠真实使用暴露问题」：

1. 补 ADR-0001 的显示矩阵：多显示器、100%～200% DPI、任务栏自动隐藏与左对齐、Explorer 重启、全屏。本轮只验证了参考机的底部居中任务栏。
   同时补跑 `scripts/Test-LauncherEntryPlacement.ps1` 的点击一段：本轮该机拒绝一切合成输入（`SetCursorPos` 返回 `FALSE` 且不设错误码，
   `SendInput` 报成功但指针不动），入口的窗口过程改为由 `PostMessage` 直接驱动验证，几何与穿透两段仍按原脚本通过。
2. **真实使用一段时间**，只记录可复现缺陷。本轮两个缺陷都由实际运行暴露，不是读代码发现的。
3. `MainWindow.xaml.cs` 的便签删除/编辑、拖动和设置协调**等下次真要改这些行为时顺带拆**，不单独开一轮；重构回报取决于后续还要改多少代码。
4. 常驻已落地（见 §4.2），孤儿进程已由 Job Object 根治（见 §6）。剩余相关项：重新显示沿用启动时解析的几何，跨显示器或 DPI 变化后的重显尚未验证；`PublishReadyToRun` 的发布配置仍未修好。
5. `ProviderRefreshScheduler.cs` 仅在其开始产生缺陷时再分解。

## 8. 延期

- 计时器、待办、剪贴板、日历和系统监控业务；
- 自动定位和跨重启天气 payload 缓存；
- Outlook、Google 和 Microsoft To Do；
- PluginHost、第三方插件和声明式 UI；
- 云同步、账号和遥测；
- 多布局场景和高级动效；
- 安装、更新、签名、SBOM 和企业发布门禁。

延期项不得作为当前“下一步”自动实施。

## 9. 方向与待用户决定

方向已定（2026-08-31）：目标是成为一个优秀的**开源工具**，面向公开发布。由此新增的发布前置项：许可证选择与依赖许可证复核、面向外部读者的 README、可工作的发布产物（`PublishReadyToRun` / `resources.pri` 发布配置仍是坏的，见 §4.2）、贡献与安全报告说明。

同日完成一轮**适配性审计**（产品原则 7），结论并入发布前置项：

- **单位制硬编码**：天气请求固定 `temperature_unit=celsius`、`wind_speed_unit=kmh`（`OpenMeteoWeatherProvider.BuildRequestUri`），华氏/英里用户无法切换，需要单位设置；
- **无 Windows 版本门槛**：LauncherHost 不检查系统版本。条带推导假设 Windows 11 任务栏形态，Windows 10 满排按钮的任务栏上不成立，会错位或盖住按钮；需启动时显式检查并降级（悬浮或拒绝，说明原因）。第三方任务栏改造（ExplorerPatcher 类）依赖既有 fail-closed，未实测；
- **语言仅 en-US / zh-CN**：`DefaultLanguage` 为 en-US，其他区域回退英文；Broker 组的读数字符串一律 `InvariantCulture`（小数点恒为 `.`），金额恒为 USD。可接受为初始状态，翻译列为贡献面；
- **常用地点列表以中国大陆城市为主**（19/33）：有搜索与自动定位兜底，低优先；
- **仅 x64**：Arm64 Windows 依赖 x64 仿真，未验证；Arm64 原生构建列为已知限制与贡献面；
- **价格表随发布物老化**：`TokenUsagePricing.VerifiedOn` 已入库但不用于展示；发布物比源码活得久，价表过老时卡片应有提示；
- 显示矩阵（多显示器、100%～200% DPI、自动隐藏/垂直任务栏）仍是发布候选门禁，开源用户环境的第一波缺陷预计来自这里。

审计确认已适配良好、无需改动的部分：`PdhAddEnglishCounter`（非英文 Windows）；缺失计数器与传感器逐项降级为明确状态；GPU 按 engine type 取最大、iGPU 共享显存合计；时钟与日期经 `GetTimeFormatEx`/`GetDateFormatEx` 随用户区域；入口零可翻译文本；天气描述按语言取资源键；Token 厂商目录可用性探测、无价模型计数不瞒报；网卡枚举过滤与尖峰保护；定位拒绝/无数据降级且验收路径不请求位置。

仍待用户决定：

- 正式名称与图标；
- 最终许可证；
- 何时进入发布候选阶段。
